
# Python
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/

# Environment / secrets
.env

# Data (never committed — see docs/dataset.md for how to obtain it)
data/raw/
data/processed/*
data/processed/*
!data/processed/eda_stats.json
!data/processed/baseline_results.json
!data/processed/transformer_results.json
!data/processed/transformer_full_results.json

...

# Model checkpoints (tracked via MLflow registry instead)
*.pt
*.bin
*.safetensors


# MLflow local tracking artifacts
mlruns/
mlflow.db

# Jupyter
.ipynb_checkpoints/

# Model checkpoints (tracked via MLflow registry instead)
*.pt
*.bin
*.safetensors

# macOS
.DS_Store