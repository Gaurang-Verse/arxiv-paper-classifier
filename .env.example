# Copy to .env for local overrides. .env is gitignored.
#
# Nothing is required to run this project today: MLflow writes to a local
# SQLite file (mlflow.db) and all other settings live in configs/*.yaml.
# Keep any future secrets (API keys, remote tracking URIs) here, never in code.
